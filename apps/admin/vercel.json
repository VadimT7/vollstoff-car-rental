{
  "framework": "nextjs",
  "installCommand": "pnpm install --prod=false",
  "buildCommand": "pnpm -w db:generate && ( [ -d apps/admin ] && pnpm --dir apps/admin exec next build || pnpm exec next build )",
  "outputDirectory": ".next"
}
